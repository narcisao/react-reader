import React, { Fragment } from 'react';

import Header from './Header';

const NotFound = () => {
    return (
        <Fragment>
            <Header />
            <h1>Não há nada para ser descoberto aqui!</h1>
        </Fragment>
    );
}
export default NotFound;